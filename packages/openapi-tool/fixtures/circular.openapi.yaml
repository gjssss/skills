openapi: 3.0.3
info:
  title: Circular API
  version: 1.0.0
paths:
  /nodes:
    get:
      summary: Get node
      operationId: getNode
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Node'
components:
  schemas:
    Node:
      type: object
      properties:
        child:
          $ref: '#/components/schemas/Node'
